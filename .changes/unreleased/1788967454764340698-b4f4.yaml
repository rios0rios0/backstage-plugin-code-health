kind: 'Security'
body: 'raised `js-yaml` to `3.15.2` on the 3.x line and `4.3.2` on the 4.x line, and `svgo` to `2.8.4`, all through `resolutions`, to clear the three advisories `sca:yarn-audit` began failing on: GHSA-2883-xcg3-v3hh in `js-yaml`, reached through `yml-loader` on the 3.x line and `@backstage/core-components` on the 4.x line, and GHSA-w27v-7q3p-w38r in `svgo`, reached through `postcss-svgo`. The existing `js-yaml` resolutions pinned `^4.3.1`, one release below the fix, and the 3.x line had none. Every target was published more than a week ago, so `npmMinimalAgeGate: ''7d''` lets it through'
time: '2026-09-09T15:24:14.764235098Z'

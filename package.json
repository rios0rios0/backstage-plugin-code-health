{
  "name": "backstage-plugin-code-health-root",
  "version": "4.0.0",
  "description": "Monorepo root for the Code Health Backstage plugin packages",
  "license": "MIT",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "workspaces": [
    "plugins/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rios0rios0/backstage-plugin-code-health.git"
  },
  "homepage": "https://github.com/rios0rios0/backstage-plugin-code-health#readme",
  "bugs": "https://github.com/rios0rios0/backstage-plugin-code-health/issues",
  "scripts": {
    "build": "tsc && backstage-cli repo build --all",
    "typecheck": "tsc",
    "clean": "backstage-cli repo clean && rm -rf dist-types coverage",
    "lint": "backstage-cli repo lint --fix",
    "lint:ci": "backstage-cli repo lint",
    "test": "backstage-cli repo test --watch=false",
    "test:ci": "backstage-cli repo test --watch=false --coverage --reporters=default --reporters=jest-junit",
    "test:watch": "backstage-cli repo test"
  },
  "//resolutions": "Patched versions of transitive build-tooling dependencies carrying published advisories. Every target is the version GitHub records as the first patched release for the advisory in question. Each key repeats the descriptor its dependents actually declare, so a line that stops matching becomes a no-op instead of dragging an unrelated version range along with it. Yarn only honours `resolutions` declared by the workspace root, so this block cannot move into a package.",
  "resolutions": {
    "brace-expansion@npm:^1.1.7": "^1.1.18",
    "brace-expansion@npm:^2.0.1": "^2.1.4",
    "brace-expansion@npm:^2.0.2": "^2.1.4",
    "brace-expansion@npm:^5.0.2": "^5.0.9",
    "brace-expansion@npm:^5.0.5": "^5.0.9",
    "js-yaml@npm:^4.1.0": "^4.3.1",
    "js-yaml@npm:^4.2.0": "^4.3.1",
    "tar@npm:^7.5.4": "^7.5.19",
    "tar@npm:^7.5.6": "^7.5.19",
    "fast-uri@npm:^3.0.1": "^3.1.4",
    "adm-zip@npm:0.5.10": "^0.6.0",
    "prismjs@npm:~1.27.0": "^1.30.0",
    "undici@npm:7.28.0": "^7.29.0",
    "nanoid@npm:^3.3.16": "^3.3.18",
    "nanoid@npm:^3.3.17": "^3.3.18"
  },
  "devDependencies": {
    "@backstage/cli": "^0.36.4",
    "@backstage/cli-defaults": "^0.1.4",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.2",
    "jest": "^30.4.2",
    "jest-junit": "^17.0.0",
    "typescript": "~5.9.3"
  },
  "//jest": "`backstage-cli repo test` splits this block in two: keys Jest accepts per project (`collectCoverageFrom`, `roots`, …) are forwarded to every workspace package and then overridden by that package's own `jest` block, while the rest (`coverageThreshold`, `coverageReporters`) stay global and are evaluated against the merged report. Per-package exclusions therefore live in each package's `package.json`, because their paths are relative to `<package>/src`. Jest validates the keys inside `jest`, so this note lives at the root instead.",
  "jest": {
    "coverageReporters": [
      "text",
      "json",
      "json-summary",
      "html"
    ],
    "coverageThreshold": {
      "global": {
        "lines": 95,
        "functions": 92,
        "branches": 88,
        "statements": 95
      }
    }
  },
  "jest-junit": {
    "outputName": "junit-report.xml"
  }
}

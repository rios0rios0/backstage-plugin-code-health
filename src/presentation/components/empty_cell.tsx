import Typography from "@material-ui/core/Typography";

/** Placeholder rendered when a table cell has no value. */
export const EmptyCell = () => (
  <Typography variant="caption" color="textSecondary">
    -
  </Typography>
);
